<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Post Detail</title>
  <link rel="stylesheet" href="/styles/post_detail.css">
</head>
<body>
  <div class="container">
    <div class="post-card">
      <div class="post-img">
        <% if (post.imageURL) { %>
          <img src="<%= post.imageURL %>" alt="Post Image">
        <% } %>
        <h1><%= post.Title %></h1>
      </div>
      <div class="post-details">
        <span><i class="fa fa-calendar"></i><%= new Date(post.Posted_on).toLocaleDateString() %></span>
        <span><i class="fa fa-heart"></i><%= post.Likes %></span>
      </div>
      <div class="post-text">
        <p><%= post.Description %></p>
      </div>
      <div class="post-actions">
        <button class="like-button">Like (<%= post.Likes %>)</button>
        <button class="dislike-button">Dislike (<%= post.Dislikes %>)</button>
        <button class="comment-button">Comment</button>
      </div>
    </div>
  </div>
</body>
</html>
